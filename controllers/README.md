# Controllers

User-facing control clients:

- `phone/` is the browser application for phone control and live-video playback.
- `leader_arm/` is the command-line physical leader-arm client.

Neither controller owns follower hardware or hosted relay infrastructure.
